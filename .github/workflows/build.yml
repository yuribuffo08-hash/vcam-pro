name: Build VCamPro

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            bash curl build-essential fakeroot git perl dpkg xz-utils unzip zip rsync

      - name: Setup Theos and SDKs
        run: |
          export THEOS=$HOME/theos
          git clone --recursive https://github.com/theos/theos.git $THEOS
          git clone --depth=1 https://github.com/theos/sdks.git /tmp/sdks
          cp -r /tmp/sdks/* $THEOS/sdks/
          curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos | bash
          echo "THEOS=$HOME/theos" >> $GITHUB_ENV

      - name: Compile Debian Package
        run: |
          export THEOS=$HOME/theos
          chmod +x layout/DEBIAN/postinst || true
          make clean
          make package FINALPACKAGE=1

      - name: Upload .deb Artifact
        uses: actions/upload-artifact@v4
        with:
          name: VCamPro-DEB
          path: packages/*.deb
